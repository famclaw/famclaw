TASK: Audit feat/promptbuilder-skeleton-v3, fill any gaps vs the plan, rebase onto origin/main, push, open PR.

CONTEXT:
- Branch: feat/promptbuilder-skeleton-v3 (existing, has prior local-LLM-drafted work)
- Why: closes the kid-facing "I can't execute code" bug. PromptBuilder skeleton (PR #114) is on main; the 12-component completion + agent wiring + token-budget tests sit on this branch unpushed.

SPEC:
1. Audit the branch. Run: git -C wt-pb log --oneline origin/main..HEAD. Expect commits adding policy/approvals/capabilities/skills/gateway/output/memory/oauth components, the agent default-prompt wiring, token-budget regression tests, and a CHANGELOG entry.
2. List every component currently implemented in internal/prompt/components.go on this branch; map against the 12 slots defined by the BuildContext struct. Document the gap (likely zero).
3. If a slot is empty, implement it following the existing component pattern: func <Slot>(ctx BuildContext) (string, bool). Read identity.go / user.go / family.go / age.go for the reference shape — do NOT paraphrase.
4. Snapshot tests: ensure internal/prompt/testdata/{parent,age_13_17,age_8_12,under_8}.snap exist. Regenerate via UPDATE_PROMPT_SNAPSHOTS=1 go test ./internal/prompt/ if any are missing or stale.
5. Rebase the branch onto current origin/main; resolve conflicts (likely CHANGELOG only — main has #117's test-infra + #116's web_fetch entries).

CONSTRAINTS:
- Do NOT re-implement what's already on the branch — audit first
- Do NOT touch internal/agent/agent.go beyond what's already wired in commit 8045aba
- Do NOT change the BuildContext struct
- Do NOT delete or rename existing components
- Style: existing components in components.go are the template

ACCEPTANCE GATES:
1. CGO_ENABLED=0 go build ./...
2. CGO_ENABLED=0 go vet ./...
3. CGO_ENABLED=0 go test ./internal/prompt/... -count=1
4. CGO_ENABLED=0 go test ./... -count=1
5. Parent snapshot ≤900 tokens; child snapshots ≤650 tokens (the existing token-budget tests in commit 9206917 enforce this — they must pass)
6. PR opens with all CI checks queued

DELIVERABLES:
- Files modified (only if audit finds gaps): internal/prompt/components.go
- Files created (only if missing): internal/prompt/testdata/{parent,age_13_17,age_8_12,under_8}.snap
- PR opened against famclaw/famclaw, base main; do NOT merge.

EXECUTION CONTEXT (from runner):
- You are cwd inside a fresh git worktree at origin/main. Do NOT git worktree add. Do NOT cd elsewhere.
- All paths are RELATIVE to the current directory.
- Read CLAUDE.md and AGENTS.md from the repo root before doing anything else.
- After all changes, run gates via the sub_gates subrecipe.
- Do NOT git add, commit, push, or open a PR. The runner does that after gates pass.
- Final line MUST be exactly "ALL GATES PASSED" or "GATE FAILED: <which>".
