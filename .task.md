TASK: Add the unknown-account linking UI to the parent dashboard.

CONTEXT:
- Branch: feat/111-dashboard-ui
- Why: backend (PR #113) records strangers messaging the bot, but parents have no UI to link them. Closes journal finding #10. Without this UI, unknown accounts sit in limbo and the issue from the 2026-05-01 first deployment is not actually fixed.

SPEC:
1. Add an "Unknown Accounts" section to internal/web/static/index.html on the parent dashboard. Insert after the existing accounts/users list. Reuse existing dashboard CSS classes — do not introduce a new style sheet.
2. Add a <table id="unknown-accounts-table"> with columns: Gateway, Display name, First seen at, Action.
3. Add JS function loadUnknownAccounts() that fetches GET /api/unknown_accounts (existing endpoint per PR #113), populates the table.
4. Each row's Action cell renders a <select id="link-target-{accountId}"> populated from GET /api/users (existing) plus a "Link" button.
5. Add JS function linkUnknownAccount(accountId, userId) that POSTs to /api/unknown_accounts/{accountId}/link with body {"user_id": userId}; on 200 OK remove the row and refresh the user list.
6. Hook into the existing SSE stream — when an event of type "unknown_account_added" arrives, call loadUnknownAccounts().
7. ALL field assignments from items[]/users[] use .textContent or option.value/.textContent. NEVER innerHTML for any user-controlled field.
8. ALL click handlers attach via addEventListener('click', ...). NEVER inline onclick attributes.

CONSTRAINTS:
- Do NOT modify any Go file — backend endpoints already exist per PR #113
- Do NOT change the SSE stream protocol; only subscribe to a new event type
- Do NOT add new dependencies — vanilla JS, no React, no jQuery
- Do NOT touch the wizard or the chat view
- Style: match existing index.html structure, indentation, and naming

ACCEPTANCE GATES:
1. CGO_ENABLED=0 go build ./... (sanity sweep — no Go changed)
2. CGO_ENABLED=0 go test ./... -count=1
3. In the diff, grep -c "innerHTML" the new code in internal/web/static/index.html returns 0
4. In the diff, grep -c "onclick=" the new code in internal/web/static/index.html returns 0
5. node -e "$(awk '/<script>/,/<\/script>/' internal/web/static/index.html | grep -v '<script>' | grep -v '</script>')" exits 0 (JS parses cleanly — install node if missing or skip if not on PATH and report)
6. PR opens with all CI checks queued

DELIVERABLES:
- Files modified: internal/web/static/index.html, CHANGELOG.md
- Files created: none
- PR opened. Manual browser smoke (open dashboard, verify a fake unknown account row appears, click Link, verify gateway_accounts row created, browser console clean) is NOT a mechanical gate — flag in the PR description as the only outstanding manual check before merge.

EXECUTION CONTEXT (from runner):
- You are cwd inside a fresh git worktree at origin/main. Do NOT git worktree add. Do NOT cd elsewhere.
- All paths are RELATIVE to the current directory.
- Read CLAUDE.md and AGENTS.md from the repo root before doing anything else.
- After all changes, run gates via the sub_gates subrecipe.
- Do NOT git add, commit, push, or open a PR. The runner does that after gates pass.
- Final line MUST be exactly "ALL GATES PASSED" or "GATE FAILED: <which>".
